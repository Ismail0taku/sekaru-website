services:
  - type: web
    name: sekaru
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
      - key: MASTER_PASSWORD
        sync: false
